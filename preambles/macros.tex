%% macros.tex — paper-specific shorthands.
%% Keep custom \newcommand definitions here so the body text stays clean.

% Example: a bold run-in mini-heading without a numbered section.
\newcommand{\mysubsection}[1]{\vspace{0.4em}\noindent\textbf{#1}\quad}

% Example: highlight in-progress edits during drafting (remove for camera-ready).
\newcommand{\changes}[1]{\textcolor{kaistblue}{#1}}
